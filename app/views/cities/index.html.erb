<a href="/rooms" style="float: left;";><img src="/61972.png" width=30px;height=30px;></a>
<% if user_signed_in? && (can? :create, City.new)%>
<a href="/cities/new" style="float:right;"><img src="/add.png" width=30px;height=30px;></a>
<% end %>
</br></br></br>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cities.each do |city| %>
      <tr>
        <td><%= link_to city.name, city %></td>
           </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @cities %>