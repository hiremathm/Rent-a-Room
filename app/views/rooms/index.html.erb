
<div class="container">
	<% if user_signed_in? && (can? :create, Room.new)%>
  		<%= link_to new_room_path do %>
  		<i class = "glyphicon glyphicon-plus" style="float:right">Add Room</i>
  		<% end %>
	<% end %>
	<div class = "col-md-6">
	<div id="rooms">
		<center>
			<%City.all.each do |city|%>
				<strong><%= check_box_tag city.name, city.id %><label for="<%= city.name %>"><%=city.name %></strong>
			<%end%>
		</center>
	</div>
	<br/>
	<ul>
	<table>
		<thead>		
		</thead>
		<tbody id="roomlist">
			
				<% @r.each do |room| %>
					<% if room.is_authorized == true %>
					<tr>	
						<td><li></b> <%= link_to room.name, room_path(room.slug) %></li></td>
					</tr>
					<% end %>
				<% end %>
			
		</tbody>
	</table>
	</ul>
<%= will_paginate @r %>
</div>
<br/><br/>
<div class ="col-md-6">
<!--  ******************************code for map************************************* -->

<title>Google Maps Multiple Markers</title>
  <script src="http://maps.google.com/maps/api/js?key=AIzaSyCzbuY_r3-Uj_RIBsxfKP0WqluXS8dTo5o"></script>
  <div id="map" style="height: 400px; width: 500px;">
	</div>
	<script type="text/javascript">
		  var locations = [];
   
  <% @r.each do |room|%>
    locations.push([<%= room.latitude %>,<%= room.longitude %>])
  <%end%>
  // console.log(locations);

    var map = new google.maps.Map(document.getElementById('map'), {
      zoom: 4,
      center: new google.maps.LatLng(21.146633, 79.088860),
      mapTypeId: google.maps.MapTypeId.ROADMAP
    });

    var infowindow = new google.maps.InfoWindow();

    var marker, i;

    for (i = 0; i < locations.length; i++) { 
      marker = new google.maps.Marker({
        position: new google.maps.LatLng(locations[i][0], locations[i][1]),
        map: map
      });

      google.maps.event.addListener(marker, 'click', (function(marker, i) {
        return function() {
          infowindow.setContent(locations[i][0]);
          infowindow.open(map, marker);
        }
      })(marker, i));
    }

	</script>

</div>
</div>
 <script>

 
 // *******************************************************************************
	var roomsHandle = document.getElementById('rooms');
	var roominputs = roomsHandle.getElementsByTagName('input');
	var roomListHandle = document.getElementById('roomlist');
	
	function buildProductHtml(rooms){
		roomlist.innerHTML = "";
		rooms.forEach(function(room){
			var li = document.createElement('li');
			var a = document.createElement('a');
			var href = document.createAttribute('href');
			href.value = `/rooms${room.id}`;
			a.setAttributeNode(href);
			var text = document.createTextNode(room.name);
			a.appendChild(text);
			li.appendChild(a);
			roomListHandle.appendChild(li);
		});
	}
	
	roomsHandle.addEventListener('click', function(){
		var roomselected = [];
		for(var i = 0; i < roominputs.length; i++){
			if(roominputs[i].checked){
				roomselected.push(roominputs[i].value);
			}
		}
		var xhr = new XMLHttpRequest();
		xhr.open('GET', '../rooms/find_by_cities?city_ids=' + roomselected.join(","));
		xhr.onreadystatechange = function(){
			if(xhr.readyState == 4 && xhr.status == 200){
				buildProductHtml(JSON.parse(xhr.responseText));
			}
		}
		xhr.send();
	}, false);

var LowHandle = document.getElementById('Low');
var HighHandle = document.getElementById('High');

LowHandle.addEventListener('change',function(){
		var xhr = new XMLHttpRequest();
		xhr.open('GET','../rooms/by_price_asc?low=');
		xhr.onreadystatechange = function(){
			if(xhr.readyState == 4 && xhr.status == 200 ){
				buildProductHtml(JSON.parse(xhr.responseText));
			}
		}
		xhr.send();
	},false);

	HighHandle.addEventListener('change',function(){
		var xhr = new XMLHttpRequest();
		xhr.open('GET','../rooms/by_price_desc?high=');
		xhr.onreadystatechange = function(){
			if(xhr.readyState == 4 && xhr.status == 200 ){
				buildProductHtml(JSON.parse(xhr.responseText));
			}
		}
		xhr.send();
	},false);
</script>

